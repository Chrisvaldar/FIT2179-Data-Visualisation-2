# Chart specs

Vega and Vega-Lite specifications grouped by narrative section. Each spec uses `data/` paths resolved from the site root (see `embedChart` in `index.html`).

| Section | Files |
|---------|-------|
| **section-1** | `dot_map.vg.json`, `state_choropleth_rate.vg.json`, `state_choropleth_fatal.vg.json`, `decade_timeline.vg.json` |
| **section-2** | `species_sankey.vg.json`, `species_size_icons.vg.json`, `wobbegong_activity.vg.json` |
| **section-3** | `activity_risk.vg.json`, `radial_time_clock.vg.json` |
| **section-4** | `gender_isotype.vg.json`, `age_mirror_ridge.vg.json` |
| **section-5** | `nsw_dot_map.vg.json`, `ca_dot_map.vg.json`, `outcome_comparison.vg.json` |

Embedded from `index.html` via `charts/section-N/<spec>.vg.json`.
